android {
            defaultConfig {
                applicationId = "org.nativescript.vhltest"

                if (false) {
                    minSdkVersion = false
                }

                if (false) {
                    targetSdkVersion = false
                }
            }
        }